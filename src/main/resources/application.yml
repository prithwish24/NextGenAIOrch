spring:
   application:
      name: nextgen-ai-service
   mvc:
      view:
         prefix: /WEB-INF/views/
         suffix: .jsp
      static-path-pattern: /resources/**

server:
   port: ${PORT:8080} 
   
   