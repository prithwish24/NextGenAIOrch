spring:
   application:
      name: nextgen-ai-service
   mvc:
      view:
         prefix: /WEB-INF/views/
         suffix: .jsp


server:
   port: ${PORT:8080} 
   
logging:
   level:
      org.springframework.web: INFO

      